<Page x:Class="BlogTools.ManagePostsPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
      Foreground="{DynamicResource TextFillColorPrimaryBrush}"
      Title="文章管理">
    <Page.Triggers>
        <EventTrigger RoutedEvent="Page.Loaded">
            <BeginStoryboard>
                <Storyboard>
                    <DoubleAnimation Storyboard.TargetName="MainContent" Storyboard.TargetProperty="Opacity" From="0" To="1" Duration="0:0:0.8" />
                    <ThicknessAnimation Storyboard.TargetName="MainContent" Storyboard.TargetProperty="Margin" From="0,40,0,0" To="16" Duration="0:0:0.8">
                        <ThicknessAnimation.EasingFunction>
                            <CubicEase EasingMode="EaseOut" />
                        </ThicknessAnimation.EasingFunction>
                    </ThicknessAnimation>
                </Storyboard>
            </BeginStoryboard>
        </EventTrigger>
    </Page.Triggers>
    <Grid Margin="16" x:Name="MainContent" Opacity="0" Background="Transparent">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="文章管理与检索" FontSize="24" FontWeight="SemiBold" Margin="0,0,0,16"/>
        
        <ui:TextBox Grid.Row="1" x:Name="SearchBox" PlaceholderText="输入文章标题或文件名进行全局搜索..." Margin="0,0,0,16" TextChanged="SearchBox_TextChanged" Icon="{ui:SymbolIcon Symbol=Search24}"/>

        <DataGrid Grid.Row="2" x:Name="PostsGrid" AutoGenerateColumns="False" HeadersVisibility="Column" SelectionMode="Single" 
                  MouseDoubleClick="PostsGrid_MouseDoubleClick" PreviewMouseWheel="PostsGrid_PreviewMouseWheel"
                  Background="Transparent" CanUserResizeRows="False" GridLinesVisibility="Horizontal"
                  BorderThickness="1" BorderBrush="{DynamicResource ControlStrokeColorDefaultBrush}">
            <DataGrid.Columns>
                <DataGridTextColumn Header="发布日期" Binding="{Binding Date, StringFormat={}{0:yyyy-MM-dd}}" Width="120" IsReadOnly="True"/>
                <DataGridTextColumn Header="文章标题" Binding="{Binding Title}" Width="*" IsReadOnly="True"/>
                <DataGridTextColumn Header="源文件名" Binding="{Binding FileName}" Width="250" IsReadOnly="True">
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock">
                            <Setter Property="Foreground" Value="{DynamicResource TextFillColorTertiaryBrush}"/>
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>
                <DataGridTemplateColumn Header="操作" Width="100">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                <ui:Button Icon="{ui:SymbolIcon Symbol=Edit24}" Background="Transparent" BorderThickness="0" ToolTip="编辑文章" Click="EditPost_Click"/>
                                <ui:Button Icon="{ui:SymbolIcon Symbol=Delete24}" Background="Transparent" BorderThickness="0" ToolTip="删除文章" Foreground="{DynamicResource SystemFillColorCriticalBrush}" Click="DeletePost_Click" Margin="8,0,0,0"/>
                            </StackPanel>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Page>

