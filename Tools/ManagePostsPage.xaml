<Page x:Class="BlogTools.ManagePostsPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
      xmlns:s="clr-namespace:System.Windows;assembly=PresentationFramework"
      Foreground="{DynamicResource TextFillColorPrimaryBrush}"
      Title="{DynamicResource NavManagePosts}">
    <Page.Triggers>
        <EventTrigger RoutedEvent="Page.Loaded">
            <BeginStoryboard>
                <Storyboard>
                    <DoubleAnimation Storyboard.TargetName="MainContent" Storyboard.TargetProperty="Opacity" From="0" To="1" Duration="0:0:0.8" />
                    <ThicknessAnimation Storyboard.TargetName="MainContent" Storyboard.TargetProperty="Margin" From="0,40,0,0" To="16" Duration="0:0:0.8">
                        <ThicknessAnimation.EasingFunction>
                            <CubicEase EasingMode="EaseOut" />
                        </ThicknessAnimation.EasingFunction>
                    </ThicknessAnimation>
                </Storyboard>
            </BeginStoryboard>
        </EventTrigger>
    </Page.Triggers>

    <Page.Resources>
        <Style x:Key="ManagePostsDataGridStyle" TargetType="DataGrid">
            <Setter Property="AutoGenerateColumns" Value="False" />
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="Foreground" Value="{DynamicResource TextFillColorPrimaryBrush}" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Padding" Value="0" />
            <Setter Property="FontFamily" Value="{x:Static s:SystemFonts.MessageFontFamily}" />
            <Setter Property="IsManipulationEnabled" Value="True" />
            <Setter Property="Stylus.IsFlicksEnabled" Value="False" />
            <Setter Property="HeadersVisibility" Value="Column" />
            <Setter Property="CanUserAddRows" Value="False" />
            <Setter Property="CanUserDeleteRows" Value="False" />
            <Setter Property="CanUserReorderColumns" Value="True" />
            <Setter Property="CanUserResizeColumns" Value="True" />
            <Setter Property="CanUserResizeRows" Value="False" />
            <Setter Property="CanUserSortColumns" Value="True" />
            <Setter Property="IsReadOnly" Value="True" />
            <Setter Property="SelectionMode" Value="Single" />
            <Setter Property="SelectionUnit" Value="FullRow" />
            <Setter Property="RowHeaderWidth" Value="0" />
            <Setter Property="GridLinesVisibility" Value="Horizontal" />
            <Setter Property="HorizontalGridLinesBrush" Value="{DynamicResource ControlStrokeColorDefaultBrush}" />
            <Setter Property="VerticalGridLinesBrush" Value="Transparent" />
            <Setter Property="ScrollViewer.CanContentScroll" Value="False" />
            <Setter Property="ScrollViewer.HorizontalScrollBarVisibility" Value="Auto" />
            <Setter Property="ScrollViewer.VerticalScrollBarVisibility" Value="Auto" />
        </Style>

        <Style x:Key="ManagePostsDataGridColumnHeaderStyle" TargetType="DataGridColumnHeader">
            <Setter Property="Background" Value="{DynamicResource CardBackgroundFillColorSecondaryBrush}" />
            <Setter Property="Foreground" Value="{DynamicResource TextFillColorPrimaryBrush}" />
            <Setter Property="BorderBrush" Value="{DynamicResource ControlStrokeColorDefaultBrush}" />
            <Setter Property="BorderThickness" Value="0,0,1,1" />
            <Setter Property="Padding" Value="16,11" />
            <Setter Property="HorizontalContentAlignment" Value="Left" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="FontFamily" Value="{x:Static s:SystemFonts.MessageFontFamily}" />
        </Style>

        <Style x:Key="ManagePostsDataGridRowStyle" TargetType="DataGridRow">
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="BorderBrush" Value="{DynamicResource ControlStrokeColorDefaultBrush}" />
            <Setter Property="BorderThickness" Value="0,0,0,1" />
            <Setter Property="SnapsToDevicePixels" Value="True" />
            <Setter Property="HorizontalContentAlignment" Value="Stretch" />
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="{DynamicResource SubtleFillColorSecondaryBrush}" />
                </Trigger>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Background" Value="{DynamicResource CardBackgroundFillColorSecondaryBrush}" />
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="ManagePostsDataGridCellStyle" TargetType="DataGridCell">
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Padding" Value="0" />
            <Setter Property="FocusVisualStyle" Value="{x:Null}" />
            <Setter Property="Foreground" Value="{DynamicResource TextFillColorPrimaryBrush}" />
            <Style.Triggers>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Background" Value="{DynamicResource CardBackgroundFillColorSecondaryBrush}" />
                    <Setter Property="Foreground" Value="{DynamicResource TextFillColorPrimaryBrush}" />
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="ManagePostsDateTextStyle" TargetType="TextBlock">
            <Setter Property="Margin" Value="16,12" />
            <Setter Property="VerticalAlignment" Value="Center" />
            <Setter Property="FontSize" Value="13" />
            <Setter Property="Foreground" Value="{DynamicResource TextFillColorSecondaryBrush}" />
            <Setter Property="TextTrimming" Value="CharacterEllipsis" />
        </Style>

        <Style x:Key="ManagePostsTitleTextStyle" TargetType="TextBlock">
            <Setter Property="Margin" Value="16,12" />
            <Setter Property="VerticalAlignment" Value="Center" />
            <Setter Property="FontSize" Value="14" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="Foreground" Value="{DynamicResource TextFillColorPrimaryBrush}" />
            <Setter Property="TextTrimming" Value="CharacterEllipsis" />
        </Style>

        <Style x:Key="ManagePostsFileNameTextStyle" TargetType="TextBlock">
            <Setter Property="Margin" Value="16,12" />
            <Setter Property="VerticalAlignment" Value="Center" />
            <Setter Property="FontSize" Value="13" />
            <Setter Property="Foreground" Value="{DynamicResource TextFillColorTertiaryBrush}" />
            <Setter Property="TextTrimming" Value="CharacterEllipsis" />
        </Style>

        <DataTemplate x:Key="ManagePostsActionsTemplate">
            <Border Padding="10,8">
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                    <ui:Button Icon="{ui:SymbolIcon Symbol=Edit24}"
                               Background="Transparent"
                               BorderThickness="0"
                               Padding="8"
                               ToolTip="{DynamicResource ManagePostsTipEdit}"
                               Click="EditPost_Click" />
                    <ui:Button Icon="{ui:SymbolIcon Symbol=Delete24}"
                               Background="Transparent"
                               BorderThickness="0"
                               Padding="8"
                               ToolTip="{DynamicResource ManagePostsTipDelete}"
                               Foreground="{DynamicResource SystemFillColorCriticalBrush}"
                               Click="DeletePost_Click"
                               Margin="8,0,0,0" />
                </StackPanel>
            </Border>
        </DataTemplate>
    </Page.Resources>

    <Grid Margin="16" x:Name="MainContent" Opacity="0" Background="Transparent">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <StackPanel Margin="0,0,0,16">
            <TextBlock Text="{DynamicResource ManagePostsTitle}" FontSize="24" FontWeight="SemiBold" />
            <TextBlock Text="{DynamicResource ManagePostsSearchPlaceholder}"
                       Margin="0,4,0,0"
                       FontSize="13"
                       Foreground="{DynamicResource TextFillColorSecondaryBrush}" />
        </StackPanel>

        <ui:TextBox Grid.Row="1"
                    x:Name="SearchBox"
                    PlaceholderText="{DynamicResource ManagePostsSearchPlaceholder}"
                    Margin="0,0,0,16"
                    TextChanged="SearchBox_TextChanged"
                    Icon="{ui:SymbolIcon Symbol=Search24}" />

        <Border Grid.Row="2"
                Background="{DynamicResource CardBackgroundFillColorDefaultBrush}"
                BorderBrush="{DynamicResource ControlStrokeColorDefaultBrush}"
                BorderThickness="1"
                CornerRadius="10">
            <DataGrid x:Name="PostsDataGrid"
                      Style="{StaticResource ManagePostsDataGridStyle}"
                      RowStyle="{StaticResource ManagePostsDataGridRowStyle}"
                      CellStyle="{StaticResource ManagePostsDataGridCellStyle}"
                      ColumnHeaderStyle="{StaticResource ManagePostsDataGridColumnHeaderStyle}"
                      Sorting="PostsDataGrid_Sorting"
                      MouseDoubleClick="PostsDataGrid_MouseDoubleClick">
                <DataGrid.Columns>
                    <DataGridTextColumn x:Name="DateColumn"
                                        Header="{DynamicResource ManagePostsColDate}"
                                        Width="160"
                                        MinWidth="140"
                                        SortMemberPath="Date"
                                        Binding="{Binding Date, StringFormat={}{0:yyyy-MM-dd}}"
                                        ElementStyle="{StaticResource ManagePostsDateTextStyle}" />
                    <DataGridTextColumn x:Name="ModifiedDateColumn"
                                        Header="{DynamicResource ManagePostsColModifiedDate}"
                                        Width="160"
                                        MinWidth="140"
                                        SortMemberPath="LastModifiedAt"
                                        Binding="{Binding LastModifiedAt, StringFormat={}{0:yyyy-MM-dd}, TargetNullValue=-}"
                                        ElementStyle="{StaticResource ManagePostsDateTextStyle}" />
                    <DataGridTextColumn x:Name="TitleColumn"
                                        Header="{DynamicResource ManagePostsColTitle}"
                                        Width="2*"
                                        MinWidth="240"
                                        SortMemberPath="Title"
                                        Binding="{Binding Title}"
                                        ElementStyle="{StaticResource ManagePostsTitleTextStyle}" />
                    <DataGridTextColumn x:Name="FileNameColumn"
                                        Header="{DynamicResource ManagePostsColFileName}"
                                        Width="2*"
                                        MinWidth="220"
                                        SortMemberPath="FileName"
                                        Binding="{Binding FileName}"
                                        ElementStyle="{StaticResource ManagePostsFileNameTextStyle}" />
                    <DataGridTemplateColumn x:Name="ActionsColumn"
                                            Header="{DynamicResource ManagePostsColActions}"
                                            Width="120"
                                            MinWidth="120"
                                            CanUserSort="False"
                                            CellTemplate="{StaticResource ManagePostsActionsTemplate}" />
                </DataGrid.Columns>
            </DataGrid>
        </Border>
    </Grid>
</Page>

