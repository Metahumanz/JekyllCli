<Window x:Class="BlogTools.UpdateProgressWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="{DynamicResource SettingsMsgUpdateFound}"
        Width="420"
        Height="150"
        MinWidth="420"
        ResizeMode="NoResize"
        ShowInTaskbar="False"
        WindowStartupLocation="CenterOwner"
        Background="{DynamicResource CardBackgroundFillColorDefaultBrush}"
        Foreground="{DynamicResource TextFillColorPrimaryBrush}">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock x:Name="StatusText"
                   Text="{DynamicResource SettingsMsgProgressPreparing}"
                   FontSize="14"
                   TextWrapping="Wrap" />

        <ProgressBar x:Name="DownloadProgressBar"
                     Grid.Row="1"
                     Margin="0,16,0,0"
                     Height="8"
                     Minimum="0"
                     Maximum="100"
                     Value="0" />
    </Grid>
</Window>
