<ui:FluentWindow x:Class="BlogTools.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
        xmlns:local="clr-namespace:BlogTools"
        xmlns:icons="clr-namespace:BlogTools.Icons"
        Title="{DynamicResource AppTitle}" Height="760" Width="1200"
        WindowBackdropType="Mica"
        ExtendsContentIntoTitleBar="True"
        Foreground="{DynamicResource TextFillColorPrimaryBrush}"
        WindowStartupLocation="CenterScreen">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <ui:TitleBar Grid.Row="0" x:Name="AppTitleBar" Title="{DynamicResource AppTitle}" />
        
        <ui:NavigationView Grid.Row="1" x:Name="RootNavigation" PaneDisplayMode="Left" OpenPaneLength="200" IsBackButtonVisible="Collapsed" TransitionDuration="400">
            <ui:NavigationView.MenuItems>
                <ui:NavigationViewItem Content="{DynamicResource NavDashboard}" Icon="{icons:AssetIcon Home}" TargetPageType="{x:Type local:DashboardPage}" />
                <ui:NavigationViewItem Content="{DynamicResource NavManagePosts}" Icon="{icons:AssetIcon Grid}" TargetPageType="{x:Type local:ManagePostsPage}" />
                <ui:NavigationViewItem Content="{DynamicResource NavEditor}" Icon="{icons:AssetIcon Edit}" TargetPageType="{x:Type local:EditorPage}" />
                <ui:NavigationViewItem Content="{DynamicResource NavSettings}" Icon="{icons:AssetIcon Globe}" TargetPageType="{x:Type local:SettingsPage}" />
            </ui:NavigationView.MenuItems>
            <ui:NavigationView.FooterMenuItems>
                <ui:NavigationViewItem Content="{DynamicResource NavAppSettings}" Icon="{icons:AssetIcon Settings}" TargetPageType="{x:Type local:AppSettingsPage}" />
            </ui:NavigationView.FooterMenuItems>
        </ui:NavigationView>
    </Grid>
</ui:FluentWindow>
