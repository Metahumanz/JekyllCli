# JekyllCli 🚀

[English](docs/README_EN.md) | 简体中文 | [中文使用说明](docs/USER_GUIDE_ZH.md)
也可查看于[我的博客](https://pacil.dpdns.org/posts/JekyllCli-intro)

JekyllCli 是一款专为 Jekyll 静态博客（深度适配 [Chirpy 主题](https://github.com/cotes2020/jekyll-theme-chirpy)）打造的**现代化、高颜值 Windows 桌面管理工具集**。它结合了 Fluent Design 界面与无痛配置体验，让技术博客的运营和协作跟写本地笔记一样简单。

本项目同时提供**自带模板的完整版**与**轻量纯净版**，满足不同阶段博主的需求。

---

## ✨ 特性

- 🎨 **图标自适应切换**：引入全新设计的沉稳专业风格图标，并实现深浅色主题自适应逻辑。窗口图标与任务栏图标将随应用主题色实时切换。
- 📦 **真正单文件发布**：现在工具编译后仅为一个单一的 `JekyllCli.exe`，所有托管库、原生 DLL 以及 KaTeX 渲染引擎均已完美集成在 EXE 内部，真正做到解压即用。
- 🔄 **应用内自热更新**：新增基于“重命名防冲突破坏机制”的自更新功能。工具在启动时自动静默检查 GitHub 最新 Release，支持一键下载并在应用内完成自动替换与重启。

---

## 📖 目录

- [一、快速开始（小白直通车）](#一快速开始小白直通车)
- [二、初始配置与如何使用](#二初始配置与如何使用)
- [三、进阶：如何修改工具源码](#三进阶如何修改工具源码)
- [四、自行打包与自动化发布](#四自行打包与自动化发布)
- [五、版本更新日志](#五版本更新日志)
- [六、开源协议](#六开源协议)

---

## 一、快速开始（小白直通车）

如果您只是想用这个工具来管理您的静态博客，而不想碰底层代码，请参考以下步骤：

1. **安装环境依赖**：
   - 请确保您的 Windows 电脑上安装了 [.NET 10.0 Desktop Runtime](https://dotnet.microsoft.com/download)。
   - 需要安装并在命令行配置过 [Git](https://git-scm.com/)（工具底层会调用 Git 帮您同步云端）。
2. **下载指南 (README 必看)**：
   前往仓库的 **[Releases 页面](https://github.com/Metahumanz/JekyllCli/releases)**，根据你的需求选择文件：

   | 推荐人群 | 下载文件名 | 包含内容 | 特点 |
   | :--- | :--- | :--- | :--- |
   | **新手/快速试用** | `JekyllCli-win-x64-bundle.zip` | **工具 + 博客模板** | **最推荐**。解压即开即用，自带完整的博客基座。 |
   | **已有博客/老鸟** | `JekyllCli-win-x64-minimal.zip` | **仅工具 (单一 EXE)** | 极致精简。只有一个 EXE，适合已有博客目录的用户。 |

   > 💡 **提示**：本工具支持**自动检查更新**，一旦有新版本发布，您将在启动时收到弹窗提醒，点击即可完成无缝升级。

3. **运行工具**：
   - 解开压缩包，双击运行 `JekyllCli.exe` 即可启动。

## 二、初始配置与如何使用

如果您是**第一次启动该工具**，该如何完成博客配置呢？

### 1. 链接博客数据源
首次打开 JekyllCli 会弹出**自动适配高度的初始欢迎向导**：
- **选择博客目录**：
    - **已有博客**：直接指定包含 `_config.yml` 的本地文件夹。
    - **零基础开始**：如果你下载的是 `bundle` 版，点击“使用内置模板”即可一键完成初始化。
- **选择语言和时区**：在向导第二步下拉菜单选择你的语言。系统将自动帮助写入时区和语言设定。

### 2. 界面核心功能
- **仪表盘大盘**：实时读取本地 Markdown 统计。
- **Fluent 写作编辑**：
  - **公式支持**：内置 KaTeX 数学公式引擎，支持离线渲染。
  - **同步滚动**：预览与编辑双向定位。
- **现代化设置面板**：
  - **更新中心**：一键检测并执行应用内静默升级。
  - **静态资源上传**：支持批量上传网站 Favicons 与博客插图。

---

## 三、进阶：如何修改工具源码

本项目的桌面端是用 **C# 10 + WPF + WPF UI (.NET 10)** 编写的。

1. **环境准备**：
   - **Visual Studio 2022** / **Rider** 等集成开发环境，并装配 `.NET 桌面开发` 工作负载。
2. **理解目录结构**：
   ```text
   JekyllCli/
   ├── Blog/           // 官方干净 Chirpy Starter 博客文件存放处
   ├── Tools/          // WPF 本地客户端项目根目录
   │   ├── src/        // 页面、窗口、服务、模型与语言资源源码
   │   ├── Assets/     // 应用图标与 KaTeX 嵌入式资源
   │   └── App.xaml    // 入口配置
   ├── .github/
   └── README.md
   ```
3. **本地编译运行**：
   - 进入 `Tools` 目录执行：
   ```bash
   dotnet run
   ```

---

## 四、自行打包与自动化发布

如果您在 `Tools` 里加了私有功能，想自行发布：

### 方法一：单文件发布
命令行进入 `Tools/` 后运行命令生成单一 `.exe` 文件：
```powershell
dotnet publish
```

### 方法二：GitHub Actions 自动化 (推荐)
本项目已内置 **GitHub Actions 释放流** (`.github/workflows/release.yml`)：
- 只需要在本地推送一个以 **`v`** 开头的 Tag（如 v1.0.0）：
  ```bash
  git tag v1.0.0
  git push origin v1.0.0
  ```
- GitHub 将自动在云端完成编译、压缩并发布至 Release 页面。

---

## 五、版本更新日志

- **v1.6.6**：修复“存量管理”页面在表格区域滚轮失效、不同区域滚动手感不一致的问题，统一恢复丝滑滚动体验；同时为应用补充 `PerMonitorV2` 高分屏感知清单，改善 Windows 缩放场景下界面发糊的问题。
- **v1.6.5**：将原本混合在同一页的设置逻辑拆分为“博客设置”和“软件设置”两个独立页面；博客设置保留站点信息、导航选项卡、Favicon 与保存发布等内容，软件设置则集中管理工作区目录、撰写偏好、界面外观与应用更新，并将软件设置入口移动到左侧栏左下角，博客设置继续保留在原设置位置。
- **v1.6.4**：修复设置页全局字体选择看起来未生效的问题，所选字体现在会同步应用到主窗口、导航栏、标题栏与文章管理列表；同时移除字体下拉框的手动输入，仅保留可搜索的选择方式。
- **v1.6.3**：撰写界面新增“仅撰写 / 边写边预览 / 仅预览”三种布局模式，支持通过中栏拖拽在分栏模式下调整宽度并自动切换到单栏；重做顶部模式切换、存草稿、保存发布与清空重置按钮，使其与编辑器工具栏保持一致的视觉语言；优化文章标签输入交互，按 `Enter` 添加后自动退出输入，按 `Esc` 可放弃当前输入并立即退焦。
- **v1.6.2**：优化启动时的更新提示流程，发现新版本后可直接在弹窗中开始下载，不再跳转到设置页；新增独立下载进度窗口，实时显示下载进度，并在下载完成后继续确认是否立即应用更新。
- **v1.6.1**：修复 Markdown 工具箱按钮在暗色主题下底色不明显、图标难以辨认的问题；将一级到四级标题按钮改为稳定显示 `H1/H2/H3/H4` 文本，并将分隔线按钮改为清晰的横线图形，避免 Fluent 图标字体缺字或乱码。
- **v1.6.0**：写作区升级为可拖拽自定义的 Markdown 工具箱，支持标题/加粗/列表/代码块/表格等快捷插入，并允许将常用工具固定到中栏；新增链接插入对话框、工具悬停提示与中栏保留策略设置；优化文章管理页的滚轮/触摸滚动与“修改日期”排序，同时修复 WPF 构建中间目录权限导致的编译失败问题，并将 `Tools` 源码按页面/窗口/服务统一整理到 `src/` 目录。
- **v1.5.0**：写作区新增“插入链接”快捷按钮，可通过对话框填写显示文本与 URL，并选择当前页或新标签页打开；为常见界面组件补上统一的悬浮上浮特效，鼠标移入时交互反馈更轻盈自然。
- **v1.4.1**：全面实现软件 UI 国际化，支持中英文自适应切换；将物理引擎平滑滚动推广至全应用所有页面，带来极致丝滑的视觉体验；修复了多处硬编码中文提示。
- **v1.4.0**：新增基于物理弹簧算法（指数衰减）的设置页平滑滚动引擎；新增可选的最后修改时间自动补全功能；修复了字体下拉列表的滚动缺陷并支持搜索；支持单文件发布极简版。
- **v1.3.3**：修复 GitHub API 更新检测因为未打通系统代理在部分网络下静默失败的问题；设置界面新增跳转 Github Star 快捷入口。
- **v1.3.2**：优化重命名法的执行文件防冲突破坏机制；修复独立图标不生效的问题。
- **v1.3.1**：引入沉稳专业款新图标，实现深浅色主题图标自动适配切换逻辑。
- **v1.3.0**：全面重构构建链路，实现真正单文件发布；引入基于“执行文件重命名”的应用内自热更新。
- **v1.2.0**：优化单一 EXE 生成逻辑，剥离冗余 XML 文档，缩减发布包体积。
- **v1.1.0**：支持文章插图快捷上传、WebView2 同步滚动、Bilibili 社交配置可视化。
- **v1.0.0**：JekyllCli 正式版发布，支持博客目录绑定与基本管理功能。

---

## 六、开源协议

1. 本套件的整体架构及 WPF `Tools` 辅助程序由原始作者开发，**采用 GPLv3 协议开源**。
2. 在 `Blog` 文件夹下的[Chirpy 代码](https://github.com/cotes2020/jekyll-theme-chirpy)遵从源项目 **MIT 协议**。
